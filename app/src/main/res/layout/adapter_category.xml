<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="item"
            type="nam.tran.data.model.CategoryModel" />
    </data>

    <androidx.appcompat.widget.AppCompatTextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="@{item.name}"
        android:textColor="#FFFFFF"
        android:paddingTop="@dimen/positive_5dp"
        android:paddingBottom="@dimen/positive_5dp"
        android:paddingStart="@dimen/positive_15dp"
        android:paddingEnd="@dimen/positive_15dp"
        android:layout_marginEnd="@dimen/positive_5dp"
        android:background="@drawable/background_category"
        android:textSize="@dimen/text_size_12sp"
        tools:text="US" />
</layout>